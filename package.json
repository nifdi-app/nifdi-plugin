{
  "name": "nifdi-plugin",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Contract CI for the nifdi plugin's skills: every fenced example is executed through the pinned @nifdi/mcp server.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "test:examples": "node --test test/contract.test.mjs",
    "test:structure": "node --test test/structure.test.mjs"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  }
}
